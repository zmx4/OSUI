<UserControl x:Class="OSUI.Views.Pages.Scheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OSUI.Views.Pages"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <!-- <ColumnDefinition Width="Auto"/> -->
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                    Text="算法选择" 
                    FontSize="24" FontWeight="Bold" Margin="10"/>
                <ComboBox Grid.Column="1"
                    Width="200" Margin="10" ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm}"/>
            </Grid>
            <Grid Grid.Column="1" HorizontalAlignment="Stretch" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding AddProcessCommand}">添加</Button>
                <Button Grid.Column="1" Margin="10,0,0,0" Command="{Binding RunCommand}">运行</Button>
            </Grid>
        </Grid>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Processes}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="进程ID" Binding="{Binding Id}" IsReadOnly="True" />
                <DataGridTextColumn Header="到达时间" Binding="{Binding ArrivalTime}" />
                <DataGridTextColumn Header="服务时间" Binding="{Binding BurstTime}" />
                <DataGridTextColumn Header="开始时间" Binding="{Binding StartTime}" IsReadOnly="True" />
                <DataGridTextColumn Header="完成时间" Binding="{Binding CompletionTime}" IsReadOnly="True" />
                <DataGridTextColumn Header="周转时间" Binding="{Binding TurnaroundTime}" IsReadOnly="True" />
                <DataGridTextColumn Header="带权周转时间" Binding="{Binding WeightedTurnaroundTime, StringFormat={}{0:F2}}" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>