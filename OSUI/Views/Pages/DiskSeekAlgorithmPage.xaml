<UserControl x:Class="OSUI.Views.Pages.DiskSeekAlgorithmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OSUI.Views.Pages"
             mc:Ignorable="d"
             xmlns:conv="clr-namespace:OSUI.Converters"
             xmlns:vm="clr-namespace:OSUI.ViewModels"
             xmlns:extensions="clr-namespace:OSUI.Extensions"
             DataContext="{extensions:DI {x:Type vm:DiskSeekAlgorithmPageViewModel}}"
             d:DataContext="{d:DesignInstance vm:DiskSeekAlgorithmPageViewModel}"
             d:DesignHeight="300" d:DesignWidth="800">
    <!-- <UserControl.Resources> -->
    <!--     <ResourceDictionary> -->
    <!--         <ResourceDictionary.MergedDictionaries> -->
    <!--             <ResourceDictionary Source="/Resources/Localization/Strings.zh-CN.xaml" /> -->
    <!--             <ResourceDictionary Source="/Resources/Localization/Strings.en-US.xaml" /> -->
    <!--         </ResourceDictionary.MergedDictionaries> -->
    <!--     </ResourceDictionary> -->
    <!-- </UserControl.Resources> -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="{DynamicResource DiskSeek.Title}" FontSize="24" FontWeight="Bold" Margin="10"/>
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="{DynamicResource DiskSeek.Label.SelectAlgorithm}" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding AvailableAlgorithms}" 
                          SelectedItem="{Binding SelectedAlgorithm}" 
                          Width="80" 
                          Margin="5,0"/>
                <TextBlock Text="{DynamicResource DiskSeek.Label.StartPosition}" VerticalAlignment="Center"/>
                <TextBox
                    Width="40"
                    Margin="5,0,0,0"
                    Text="{Binding StartIndex}"></TextBox>
                <TextBlock Text="{DynamicResource DiskSeek.Label.RequestCount}" VerticalAlignment="Center"/>
                <TextBox
                    Width="40"
                    Margin="5,0,0,0"
                    Text="{Binding RequestCount}"></TextBox>
                <TextBlock Text="{DynamicResource DiskSeek.Label.RequestQueue}" VerticalAlignment="Center"/>
                <TextBox
                    Width="200"
                    Margin="5,0,0,0"
                    Text="{Binding RequestsInput}"></TextBox>
                <Button Content="{DynamicResource DiskSeek.Button.StartSimulation}" Command="{Binding StartSimulationCommand}" 
                        Margin="5,0"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="10,0">
                <TextBlock Text="{DynamicResource DiskSeek.Label.TotalSeekLength}" FontWeight="Bold" />
                <TextBlock Text="{Binding CurrentResult.TotalSeekLength}" Margin="0,0,15,0" />
                <TextBlock Text="{DynamicResource DiskSeek.Label.AvgSeekLength}" FontWeight="Bold" />
                <TextBlock Text="{Binding CurrentResult.AvgRequestLength, StringFormat=\{0:F2\}}" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Margin="10" BorderBrush="LightGray" BorderThickness="1" Background="White">
            <Canvas x:Name="ChartCanvas" SizeChanged="ChartCanvas_SizeChanged"/>
        </Border>
        
    </Grid>
</UserControl>
